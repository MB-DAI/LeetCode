class Solution {
public:
    string reverseString(string s) {
        string rs;
        for (string::reverse_iterator rit = s.rbegin(); rit != s.rend(); ++rit)
            rs += *rit;
        return rs;   
    }
};
